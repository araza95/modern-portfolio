// React Imports
import { FC, Fragment } from 'react';

interface IPackagesViewProps {}

const PackagesView: FC<IPackagesViewProps> = ({}) => {
  return <Fragment></Fragment>;
};

export default PackagesView;
